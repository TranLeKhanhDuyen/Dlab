import styled from 'styled-components/macro'

import { Flex } from 'theme/base'

const TopInstructorWrapper = styled(Flex)`
  position: absolute;
  align-items: center;
  bottom: 0;
  left: 0;
  gap: 16px;
`

export default TopInstructorWrapper
