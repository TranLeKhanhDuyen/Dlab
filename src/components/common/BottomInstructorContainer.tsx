import styled from 'styled-components/macro'

import { Flex } from 'theme/base'

const BottomInstructorWrapper = styled(Flex)`
  position: absolute;
  align-items: center;
  bottom: 0;
  left: 0;
  gap: 16px;
`

export default BottomInstructorWrapper
